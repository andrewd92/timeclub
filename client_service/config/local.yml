db:
  user: "root"
  pass: "root"
  host: "localhost"
  port: "3306"
  database: "timeclub"

service:
  name: "client-service"

server:
  port:
    http: 8084
    grpc: 9080
  host: localhost

consul:
  enabled: false